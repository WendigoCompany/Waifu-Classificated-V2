const move_to = (location, id_page) => {
    switch (location) {
        case "main":
            window.location.href=  window.location.href.replace(id_page, "index.html")
            break;

        case "gallery":
            break;

        case "main":
            break;


    }
}